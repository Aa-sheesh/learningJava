public class variables2 {
    static void main(String[] args) {
        /*
        # Variable = Reusable container for a value

        # Primitive = simple value store directly in memory(stack)
        # Reference = memory address(stack) that points to the heap(memory)
        Ex: Primitive = int,double, char, boolean
            Reference = String, array, object

         # Creating variables
         1. declaration
         2. assignment
     */
        int age=5;
        System.out.println(age);

    }
}
